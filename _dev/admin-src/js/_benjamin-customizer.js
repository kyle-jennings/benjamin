// 'use strict';

jQuery(document).ready(function($) {

  require('./checkbox-group');
  require('./load-preview-url');
  require('./refresh-alert');

  require('./sortables');

});

window.$ = jQuery;
